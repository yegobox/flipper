#!/bin/bash
# Bump app pubspec versions only for apps with staged changes in this commit.
# This avoids version conflicts when multiple people commit on the same branch.
LOCKFILE=/tmp/pre-commit.lock

cleanup() {
  rm -f "$LOCKFILE"
}
trap cleanup EXIT

if [ -d ".git/rebase-merge" ] || [ -d ".git/rebase-apply" ]; then
  echo "Rebase in progress, skipping version increment..."
  exit 0
fi

if [ -f "$LOCKFILE" ]; then
  echo "Script is already running. Exiting..."
  exit 1
fi

touch "$LOCKFILE"

REPO_ROOT=$(git rev-parse --show-toplevel)
cd "$REPO_ROOT" || exit 1

STAGED_FILES=$(git diff --cached --name-only)

app_has_staged_changes() {
  local app_rel="${1#./}"
  local file
  while IFS= read -r file; do
    [[ -z "$file" ]] && continue
    [[ "$file" == "$app_rel/"* ]] && return 0
  done <<< "$STAGED_FILES"
  return 1
}

# Configuration: Add folders to exclude (space-separated list)
EXCLUDE_FOLDERS=("node_modules" "build" "test" "packages" "open-sources")

EXCLUDE_PATTERN=""
for folder in "${EXCLUDE_FOLDERS[@]}"; do
  EXCLUDE_PATTERN+=" ! -path \"*/$folder/*\""
done

OS=$(uname)
if [[ "$OS" == "Darwin" ]]; then
  SED_INPLACE() {
    sed -i .bak "$1" "$2" && rm -f "$2.bak"
  }
else
  SED_INPLACE() {
    sed -i "$1" "$2"
  }
fi

eval "find . -type f -name \"pubspec.yaml\" $EXCLUDE_PATTERN" | while read -r PUBSPEC_PATH; do
  APP_DIR=$(dirname "$PUBSPEC_PATH")

  if ! app_has_staged_changes "$APP_DIR"; then
    echo "Skipping $APP_DIR/pubspec.yaml (no staged changes in this app)"
    continue
  fi

  cd "$APP_DIR" || continue

  echo "Processing $APP_DIR/pubspec.yaml"

  version_line=$(grep "^version: " pubspec.yaml)
  if [[ -z "$version_line" ]]; then
    echo "No version field found in $APP_DIR/pubspec.yaml"
    cd - > /dev/null
    continue
  fi

  version=$(echo "$version_line" | awk '{print $2}')

  if [[ "$version" =~ ^([0-9]+\.[0-9]+\.[0-9]+)(\+([0-9]+))?$ ]]; then
    base_version="${BASH_REMATCH[1]}"
    build_number="${BASH_REMATCH[3]}"

    if [[ -z "$build_number" ]]; then
      build_number=1
    else
      build_number=$((build_number + 1))
    fi

    if [[ "$base_version" =~ ^([0-9]+)\.([0-9]+)\.([0-9]+)$ ]]; then
      major="${BASH_REMATCH[1]}"
      minor="${BASH_REMATCH[2]}"
      patch="${BASH_REMATCH[3]}"
      new_patch=$((patch + 1))
      base_version="$major.$minor.$new_patch"
    fi

    new_version="$base_version+$build_number"
  else
    echo "Invalid version format in $APP_DIR/pubspec.yaml: $version"
    cd - > /dev/null
    continue
  fi

  SED_INPLACE "s|$version_line|version: $new_version|" pubspec.yaml
  git add pubspec.yaml

  msix_version=$(awk '/msix_version:/ {print $2}' pubspec.yaml)
  if [[ -n "$msix_version" ]]; then
    if [[ "$msix_version" =~ ^([0-9]+)\.([0-9]+)\.([0-9]+)\.([0-9]+)$ ]]; then
      major="${BASH_REMATCH[1]}"
      minor="${BASH_REMATCH[2]}"
      build="${BASH_REMATCH[3]}"
      new_build=$((build + 1))
      new_msix_version="$major.$minor.$new_build.0"

      SED_INPLACE "s/msix_version: $msix_version/msix_version: $new_msix_version/" pubspec.yaml
      if [[ "$msix_version" != "$new_msix_version" ]]; then
        git add pubspec.yaml
      fi
    else
      echo "Invalid msix_version format in $APP_DIR/pubspec.yaml: $msix_version"
    fi
  fi

  cd - > /dev/null
done

exit 0
