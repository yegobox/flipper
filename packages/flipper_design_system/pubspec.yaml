name: flipper_design_system
description: Shared design tokens, theme, and primitive widgets for Flipper apps.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.10.4 <4.0.0'
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
