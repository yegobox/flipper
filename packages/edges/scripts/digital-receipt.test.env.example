# Copy to digital-receipt.test.env.local (gitignored):
#   cp scripts/digital-receipt.test.env.example scripts/digital-receipt.test.env.local

SUPABASE_PROJECT_REF=ombieopwqgfuzequezeq
SUPABASE_ANON_KEY=your_anon_key

TEST_EMAIL=you@flipper.rw
TEST_PASSWORD=your_password

# Ditto branch id (used in S3 path public/invoices-<BRANCH_ID>/...)
BRANCH_ID=7c19b5e8-1201-47cc-ae23-8fa13be6d39e

# Supabase branches.server_id — required for SMS credits (messages.branch_id)
SMS_BRANCH_ID=1

# E.164 or local digits (sendSms adds + if missing)
TEST_PHONE=250788123456

# A PDF that already exists in S3 for this branch
RECEIPT_FILE_NAME=20250515120000.pdf
