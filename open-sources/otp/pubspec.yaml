name: otp
version: 3.2.0
description: RFC6238 TOTP — vendored for Flipper (timezone dep removed; unused by lib).
homepage: https://github.com/Daegalus/dart-otp
publish_to: none

environment:
  sdk: "^3.0.0"

dependencies:
  base32: ^2.1.3
  crypto: ^3.0.0
  logging: ^1.0.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.3
